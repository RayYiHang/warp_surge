# Warp Account Manager - 快速入门指南

## 🎯 最简单使用方法

### 1️⃣ 确认安装完成
- Surge中模块已启用
- MitM已配置域名

### 2️⃣ 打开管理界面
下载并打开 `manager.html` 文件：
```
https://raw.githubusercontent.com/RayYiHang/warp_surge/refs/heads/main/manager.html
```

### 3️⃣ 添加账户
从浏览器获取账户数据，粘贴到管理界面即可。

## 🔑 如何获取账户数据

### Chrome浏览器方法：
1. 登录 https://app.warp.dev
2. 按 `F12` 打开开发者工具
3. 点击 `Application` → `Storage` → `Local Storage` → `https://app.warp.dev`
4. 复制包含 `email` 和 `stsTokenManager` 的数据

### 或者Network方法：
1. 在开发者工具中点击 `Network`
2. 刷新页面，找到登录相关请求
3. 查看请求头中的 `Authorization` 值

## ⚡ 一键命令

如果你熟悉开发者工具，可以直接在控制台执行：

```javascript
// 添加账户
accountManager.addAccount({
    "email": "your-email@example.com",
    "stsTokenManager": {
        "accessToken": "xxx",
        "refreshToken": "yyy",
        "expirationTime": 1234567890000
    }
});

// 切换账户
accountManager.setActiveAccount("your-email@example.com");

// 查看账户列表
accountManager.getAccountList();
```

## 📱 状态检查

在Surge中查看：
1. 设置 → 脚本编辑器
2. 执行：`console.log(accountManager.getAccountList())`

## 🆘 常见问题

**Q: 模块不生效？**
A: 检查MitM和域名配置，重启Surge

**Q: Token自动刷新失败？**
A: 检查refreshToken是否过期，查看控制台错误

**Q: 账户被误判封禁？**
A: 手动重置健康状态或联系开发者

---

🎉 **恭喜！你现在可以开始使用Warp多账户管理功能了！**